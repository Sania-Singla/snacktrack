// import dotenv from 'dotenv';
// import path from 'path';

// dotenv.config({
//     path:
//         process.env.ENV === 'production' ? '/etc/snacktrack_server.env' : path.resolve(process.cwd(), '.env.local'),
// });


// ✨ not needed as locally we use .env and in docker we provide env variables directly as --env-file or -e flag